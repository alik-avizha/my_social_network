import React from 'react';

const Settings = () => {
    return (
        <div>
            {/*<img src={works} alt="" className={classes.image}/>*/}
            Settings
        </div>
    );
};

export default Settings;